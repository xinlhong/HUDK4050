---
title: "ICE8_R"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```


## XINLEI HONG
## ICE8

**TASK 1**

```{R TASK1}
library(igraph)
library(tidyverse)

SNAdata <- read.csv("/Users/luke/Documents/HUDK4050/ICE8/ICE8_Data.csv", row.names = 1)

g <- graph_from_adjacency_matrix(as.matrix(SNAdata), weighted=TRUE, mode="undirected")

plot(g) 

d <- edge_density(g)
d

degree(g)

betweenness(g, normalized = TRUE)

betweenness(g, directed = FALSE, normalized = TRUE)

fc <- cluster_fast_greedy(g)
membership(fc)

sizes(fc)

V(g)$color <- fc$membership
plot(g)

V(g)$degree <- degree(g)

plot(g,
     vertex.size = V(g)$degree*2)

plot(g,
     vertex.size = V(g)$degree*2,
     edge.width = 5^(E(g)$weight)/5)

set.seed(123)

plot(g,
     vertex.size = V(g)$degree*2,
     edge.width = 5^(E(g)$weight)/5,
     layout=layout.fruchterman.reingold)
```



